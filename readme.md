# URL Shortener with FastAPI + PostgreSQL

## 📌 Features
- Shorten long URLs
- Redirect using a short code
- Frontend with TailwindCSS
- Uses psycopg (no ORM)

## 🚀 How to Run

1. **Install dependencies**  
```bash
pip install -r requirements.txt
